# QA Report — Feedback fixes 2026-05-19

**Reviewer :** Claudius (QA agent)
**Date :** 2026-05-19
**Scope :** 14 fixes (FB-001 → FB-014)
**Méthode :** Revue statique du code source (sans exécution navigateur)

---

## Résultats par feedback

### FB-001 ✅ Textures architecturales
**Fichiers :** `wall.js`, `arc-wall.js`

- Stone : hachures croisées sur ligne grise épaisse — OK
- Wood : tirets parallèles de chaque côté — OK
- Glass : ligne semi-transparente en tirets — OK
- Water : sinusoïde le long du segment — OK
- Fire : zigzag irrégulier déterministe — OK
- Grid : rendu simple inchangé (fallback `default`) — OK
- Sélection rouge : branche `selected` dédiée qui ignore `drawWallByTexture` — OK
- Même pattern appliqué à `arc-wall.js` avec `sampleArcPoints` — OK

---

### FB-002 ✅ Areas par sélection
**Fichiers :** `area-tool.js`, `area-panel.js`, `area.js`, `area-renderer.js`, `state.js`

- `window.prompt` : absent de tous les fichiers concernés — OK
- `detectAreas` / `TOGGLE_AREAS` : absents — OK
- `area-tool.js` : stub vide, plus d'interaction canvas — OK
- `area-panel.js` : bouton "Create Area from Selection" + formulaire inline avec color picker — OK
- Validation BFS dans `nodesAreConnected` : algorithme correct (adjacence restreinte + BFS depuis le 1er nœud) — OK
- `area-renderer.js` : appelle `drawAreasOverlay` sans condition `showAreas` — OK
- `ADD_AREA` présent dans `state.js` — OK

**Remarque mineure :** `fitToContent` dans `statusbar.js` itère `area.points` (ligne 124) mais les areas sont désormais stockées avec `area.nodeIds`, non `area.points`. Ce code ne plantera pas (optional chaining `?? []`) mais ne contribuera jamais rien au bounding box des areas. Risque faible — la fonctionnalité Fit reste cohérente via les `nodes`.

---

### FB-003 ✅ Clic droit termine le tracé
**Fichiers :** `wall-tool.js`, `arc-tool.js`, `separator-wall-tool.js`

- `wall-tool.js` : `onContextMenu` avec `e.preventDefault()`, reset `_chain`/`_ghostPos`, dispatch `SET_TOOL null` — OK
- `arc-tool.js` : idem, reset `_step`/`_nodeA`/`_nodeB`/`_ghostPos` — OK
- `separator-wall-tool.js` : idem, reset `_chain`/`_ghostPos` — OK
- Les 3 handlers sont enregistrés dans `initXxxTool` et désenregistrés dans `detach` — OK

---

### FB-004 ✅ Menu contextuel node
**Fichier :** `select-tool.js`

- `onContextMenu` injecté sur le canvas, `e.preventDefault()` — OK
- Menu HTML créé dynamiquement (`createElement`), positionné sur `e.clientX/Y` — OK
- Correction de débordement viewport (reposition si `rect.right > window.innerWidth`) — OK
- `deleteNodeWithWalls` : récupère tous les wall IDs via `w.from === nodeId || w.to === nodeId`, dispatch `SET_SELECTED []` — OK
- Fermeture du menu sur clic en dehors (`onDocumentClick`) — OK
- Menu fermé dans `detach()` — OK

---

### FB-005 ✅ Arc ne crée plus de mur droit
**Fichier :** `wall.js`

- Ligne 247 : `if (wall.type === WALL_TYPE.ARC) continue;` dans `drawWalls()` — OK
- Import `WALL_TYPE` depuis `schema.js` présent ligne 2 — OK

---

### FB-006 ✅ Grid size slider
**Fichiers :** `statusbar.js`, `src/index.html`

- HTML : `<input id="grid-size-input" type="range" min="16" max="128" step="8" value="64">` — OK
- `statusbar.js` : listener `input` sur slider, dispatch `SET_GRID_SIZE` — OK
- `SET_GRID_SIZE` dans `state.js` met à jour `state.ui.gridSize` et `state.map.meta.gridSize` — OK
- Sync inverse (chargement JSON → mise à jour slider) via `subscribe` — OK

---

### FB-007 ✅ Zoom erratique
**Fichiers :** `camera.js`, `statusbar.js`

- `camera.js` : `WHEEL_DELTA_CAP = 100`, deltaY cappé via `Math.max(-100, Math.min(100, e.deltaY))` — OK
- `statusbar.js` : input texte zoom (`#zoom-input`), appliqué sur Enter/blur, revert si NaN — OK
- Bouton Reset : `setZoom(camera, 1, ...)` — OK
- Input zoom focalisable avec select-all au focus — OK

---

### FB-008 ✅ Pan + Fit-to-content
**Fichiers :** `camera.js`, `statusbar.js`

- `camera.js` : `onMouseUp` sur `window` (pas canvas), reset `isPanning` pour bouton 0 ou 1 — OK
- `onBlur` sur `window` : reset `spaceDown` + `isPanning` — OK
- `fitToContent` : calcule bounding box depuis `level.nodes` et `level.decorNodes` — OK
- Calcul zoom/pan correct (centrage sur midpoint du bounding box) — OK

**Remarque (déjà signalée FB-002) :** `area.points` jamais renseigné, contribution nulle pour les areas dans Fit. Sans impact réel.

---

### FB-009 ✅ Échelle décors
**Fichiers :** `presets.js`, `decor-renderer.js`

- `ELEMENT_DIMENSIONS` défini et exporté depuis `presets.js` — OK
- `halfPx(elementType, gridSize, zoom)` : `scale = gridSize * zoom` — OK
- `drawDecorNode` utilise `halfPx` pour toutes les formes — OK
- `gridSize` résolu depuis `state.map.meta.gridSize ?? state.ui.gridSize ?? 64` — OK

---

### FB-010 ✅ Snap/free décors
**Fichiers :** `decor-tool.js`, `decor-panel.js`

- `decor-tool.js` : variable `_snapToGrid = true`, `setSnapToGrid()` exporté — OK
- `resolveWorldPos` branche sur `_snapToGrid` pour choisir snap vs world — OK
- `decor-panel.js` : checkbox `#decor-snap-checkbox` créée dynamiquement, appel `setSnapToGrid` — OK
- Default `checked = true` cohérent avec `_snapToGrid = true` — OK

---

### FB-011 ✅ Rotation décors
**Fichiers :** `schema.js`, `decor-renderer.js`, `inspector.js`

- `schema.js` : `createDecorNode` inclut `rotation: 0` — OK
- `decor-renderer.js` : `ctx.rotate(rotation)` après `ctx.translate`, conversion degrés→radians — OK
- `inspector.js` : input number `rotation` avec step=15, min=-360, max=360, dispatch `UPDATE_ENTITY` — OK

---

### FB-012 ✅ Opening découvrable
**Fichiers :** `src/index.html`, `element-panel.js`

- HTML : titre `"Wall Elements"` sur `#element-panel` — OK
- HTML : `<p id="element-panel-hint">Select a wall to add elements (door, window, opening…)</p>` — OK
- `element-panel.js` : hint masqué (`hint.hidden = hasWall`) quand mur sélectionné — OK
- Hint visible par défaut (aucun `hidden` initial dans le HTML) — OK

---

### FB-013 ✅ Éléments grisés
**Fichier :** `element-panel.js`

- `getSelectedWall()` : cherche d'abord un match direct sur `wall.id`, puis fallback sur `w.from === id || w.to === id` — OK
- Items `.disabled` quand `getSelectedWall() === null` — OK
- Clic sur item grisé : `if (!wall) return;` — OK

---

### FB-014 ✅ Prompt Design
Pas de code. Validé par définition selon les instructions du PM.

---

## Anomalies et risques

| # | Sévérité | Description | Fichier | Impact |
|---|----------|-------------|---------|--------|
| A1 | ⚠️ Mineur | `fitToContent` itère `area.points` (jamais renseigné) au lieu de `area.nodeIds`. Les areas ne contribuent pas au bounding box du bouton Fit. | `statusbar.js` L.124 | Fit ignore les areas isolées, reste fonctionnel sinon |
| A2 | ℹ️ Info | `level-panel.js` utilise encore `window.prompt` pour nommer/renommer les niveaux. Hors scope des 14 fixes, mais incohérent avec la direction prise sur FB-002. | `ui/level-panel.js` | Aucun impact sur les fixes livrés |

---

## Verdict global

**PASS ✅**

Les 14 fixes sont présents, logiquement corrects et sans régression visible sur les fonctionnalités existantes. L'anomalie A1 (Fit/areas) est un bug mineur pré-existant ou introduit par l'implémentation du schéma `nodeIds` — à corriger dans un prochain ticket mais non bloquant pour la livraison.
